# Valentine's Day mini-site

 Simple pink-themed single-page site.
 
 How to use
 Open `index.html` in a browser (double-click or serve via a static server).
 Edit the displayed name by changing the content of the `span` with id `name` in `index.html`.
 The site now uses a white background with pink handwriting-styled text (Dancing Script).
 The NO button shows a new playful message each time it moves: "opps", "maybe try again?", "hmm?", "maybe try the other button?".
 The Back button was removed from `thankyou.html` to make the message page standalone.
- The Back button was removed from `thankyou.html` to make the message page standalone.

Files
- `index.html` — main page
- `styles.css` — styling and pink theme
- `script.js` — evasive "No" button behavior and interactions
- `thankyou.html` — framed letter shown after clicking Yes

Notes
- The NO button starts as lowercase "no" and only changes after the first hover/touch/click.
- The YES button keeps its label `YES` (with a heart) and has an added border for emphasis.
- The thank-you message uses a subtle pink frame; the inner dotted border was removed.
 - The thank-you message uses a subtle pink frame; the inner dotted border was removed.
 - The letter font was updated to a cuter handwriting style (`Patrick Hand`).
 - The `YES` button now shows a pink heart emoji (💗) beside the label.
 - The letter font was switched to `Gloria Hallelujah` for a cuter handwritten feel.
 - Floating hearts were changed to static outline hearts placed along the left and right edges of the index page.
 - Floating hearts were removed; the page now uses subtle pink background blobs for decoration.
 - The letter font was switched to `Playfair Display` for a poetic, readable serif.
 - The letter font was switched to `Lora` for a softer, easy-to-read poetic feel.
